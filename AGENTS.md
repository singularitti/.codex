When you want to delete or modify anything outside the current workspace, let me know before making decisions.

When I ask you to run simulations, do not create smaller samples, trial runs, smoke tests, or other test examples unless I explicitly ask for them. If I explicitly ask you to create such test examples, delete them after the corresponding full run finishes successfully unless I explicitly ask you to keep them. Preserve the completed full-run outputs and any artifacts needed to understand, reproduce, or audit the run.

When a simulation, screening job, training job, benchmark, or other multi-step computation creates a run folder, document the folder's purpose and contents. Explain when the run folder was created, why it was created, what the run is intended to test, and what it contains. Separate inputs from outputs clearly: inputs include source files, parameters, code versions, checkpoints, datasets, and command-line options; outputs include final result tables, figures, trained models, prediction files, simulation outputs, logs, and metadata. If the run uses copied, symlinked, or in-place inputs, state that provenance explicitly. Keep temporary test artifacts separate from final outputs so they can be removed after a successful full run when I have explicitly requested those test artifacts.